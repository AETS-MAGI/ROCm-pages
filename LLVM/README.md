# LLVM

ROCm 関連で参照する LLVM 情報の入口です。

## 入口

- `llvm-index.html`

## 配下

- `LLVM-repos/llvm-repos-index.html`

## 記述方針

- 実際に参照・利用した情報のみ追記
- 将来の対応予定は `準備中 / Coming soon` として区別
- ROCm 側の観測結果と LLVM 側の推論を混同しない
